node_modules/

# Env files. ".env.local" alone does NOT match a plain ".env" or ".env.production",
# so one `git add .` would have published SMTP credentials.
.env
.env.*
!.env.example
smtp-test.js
smtp-test-verbose.js
.next/
.vercel/
.DS_Store
tsconfig.tsbuildinfo
*.tsbuildinfo
portfolio01.code-workspace
build.log
*.log

# Contact-form submissions contain PII (names, emails, phones, IPs).
# Never commit these — this repo is public.
data/
contacts.json
